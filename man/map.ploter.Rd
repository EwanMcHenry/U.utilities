% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/U_utilities_functions.R
\name{map.ploter}
\alias{map.ploter}
\title{Custom Map Plotter with ggplot2 and Optional Quantile-Based Color Limits and Plotly Integration}
\usage{
map.ploter(
  to.plot = variable,
  fill.scale.title,
  main.title,
  sub.title,
  fillground = COUNTRY.ATI.shp,
  background = COUNTRY.ATI.shp,
  transformation = "identity",
  col.limits = c(0, max(to.plot)),
  pltly.text = NULL,
  use.viridis = TRUE,
  low.col = "white",
  high.col = "red",
  fill.line_size = 0.05,
  fill.line_colour = "grey90",
  background.fill = "grey90",
  background.size = 0.05,
  background.colour = "black",
  n.breaks = 5,
  round_to = 1,
  just_pretty = TRUE,
  manual_breaks = NULL,
  quantile.probs = NULL,
  use.plotly = FALSE
)
}
\arguments{
\item{to.plot}{Numeric vector. The data values to be plotted.}

\item{fill.scale.title}{Character. The title for the fill scale (legend).}

\item{main.title}{Character. The main title of the plot.}

\item{sub.title}{Character. The subtitle of the plot.}

\item{fillground}{sf object. The spatial data to be filled and plotted.}

\item{transformation}{Character. Transformation to be applied to the fill scale. Default is "identity".}

\item{col.limits}{Numeric vector or NULL. The limits for the color scale. Default is \code{c(0, max(variable))}. Can also be determined by quantiles if \code{quantile.probs} is provided.}

\item{pltly.text}{Character or NULL. Text information to be displayed in tooltips (for interactive plots).}

\item{use.viridis}{Logical. If \code{TRUE}, the \code{viridis} color scale is used. If \code{FALSE}, a custom gradient scale from \code{low.col} to \code{high.col} is used. Default is \code{TRUE}.}

\item{low.col}{Character. The low-end color for the gradient scale (used when \code{use.viridis = FALSE}). Default is "white".}

\item{high.col}{Character. The high-end color for the gradient scale (used when \code{use.viridis = FALSE}). Default is "red".}

\item{fill.line_size}{Numeric. Size of the boundary line for the fill regions. Default is 0.05.}

\item{fill.line_colour}{Character. Color of the boundary line for the fill regions. Default is "grey90".}

\item{background.fill}{Character. Fill color for the background layer. Default is "grey90".}

\item{background.size}{Numeric. Size of the background boundary lines. Default is 0.05.}

\item{background.colour}{Character. Color of the background boundary lines. Default is "black".}

\item{n.breaks}{Numeric. Number of breaks for the color scale. Default is 5.}

\item{round_to}{Numeric. Number of decimal places to round the color breaks to. Default is 1.}

\item{just_pretty}{Logical. If \code{TRUE}, generates "pretty" breaks for the color scale.}

\item{manual_breaks}{Numeric vector. Optional manual breaks for the color scale.}

\item{quantile.probs}{Numeric vector. Optional quantile probabilities for determining color limits. Overrides \code{col.limits} if provided.}

\item{use.plotly}{Logical. If \code{TRUE}, converts the ggplot object into a plotly interactive plot. Default is \code{FALSE}.}

\item{fillground2}{sf object. A secondary spatial data layer for plotting outlines such as surrounding landmass (default is the same as \code{fillground}).}

\item{clr.breaks}{Numeric vector. The breaks for the color scale. Default is generated by \code{colour.brks}.}

\item{clr.labels}{Character vector. Labels for the color breaks. Default is generated by \code{colour.lable}.}
}
\value{
A \code{ggplot} object or a \code{plotly} interactive plot if \code{use.plotly = TRUE}.
}
\description{
This function creates a map visualization using \code{ggplot2} and supports either
a \code{viridis} color scale or a custom gradient color scale, defined by the user.
It allows for detailed customization of color breaks, labels, and plot titles.
The function also provides optional features to set color limits based on user-defined quantile probabilities of the \code{to.plot} variable and to convert the ggplot map into an interactive Plotly figure.
}
\examples{
# Example with user-defined quantile limits and interactive plot

}
