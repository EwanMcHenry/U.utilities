% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/U_utilities_functions.R
\name{map.ploter}
\alias{map.ploter}
\title{Custom Map Plotter with ggplot2, Quantile Scaling, and Optional Point Overlay}
\usage{
map.ploter(
  fillground,
  to.plot = NULL,
  fill.scale.title = NULL,
  main.title = NULL,
  sub.title = NULL,
  background = NULL,
  geom_type = c("polygon", "point"),
  categorical = NULL,
  colours = NULL,
  point.size = 2,
  point.alpha = 1,
  use.viridis = TRUE,
  low.col = "white",
  high.col = "red",
  fill.line_size = 0,
  fill.line_colour = NA,
  background.fill = "grey90",
  background.size = 0.05,
  background.colour = "black",
  n.breaks = 5,
  round_to = 1,
  just_pretty = TRUE,
  manual_breaks = NULL,
  quantile.probs = NULL,
  use.plotly = FALSE,
  pltly.text = NULL,
  transformation = "identity",
  col.limits = NULL,
  nudge_zeros = FALSE,
  min_nonzero = 0,
  point_layer = NULL,
  point_var = NULL,
  point_colours = NULL,
  point_scale.title = NULL,
  point.legend.size = 4
)
}
\arguments{
\item{fillground}{sf object. Primary spatial dataset to plot (polygons or points).}

\item{to.plot}{Character or numeric vector. Variable to map to fill (or colour if \code{geom_type = "point"}).}

\item{fill.scale.title}{Character. Title for the fill legend.}

\item{main.title}{Character. Plot title.}

\item{sub.title}{Character. Plot subtitle.}

\item{background}{sf object. Optional background layer (default = \code{fillground}).}

\item{geom_type}{Character. Either \code{"polygon"} or \code{"point"} for the main layer.}

\item{categorical}{Logical or NULL. Force categorical handling. If NULL, auto-detected.}

\item{colours}{Named vector. Colours for categorical fill values.}

\item{point.size}{Numeric. Size of plotted points.}

\item{point.alpha}{Numeric. Transparency of plotted points.}

\item{use.viridis}{Logical. Use \code{viridis} colour scale for continuous data.}

\item{low.col}{Character. Low-end colour for gradient scale (if not using viridis).}

\item{high.col}{Character. High-end colour for gradient scale.}

\item{fill.line_size}{Numeric. Line width of polygon borders.}

\item{fill.line_colour}{Character. Colour of polygon borders.}

\item{background.fill}{Character. Fill colour of background layer.}

\item{background.size}{Numeric. Line width of background borders.}

\item{background.colour}{Character. Colour of background borders.}

\item{n.breaks}{Numeric. Number of legend breaks.}

\item{round_to}{Numeric. Decimal rounding for legend labels.}

\item{just_pretty}{Logical. Use pretty breaks.}

\item{manual_breaks}{Numeric vector. Optional manual breaks.}

\item{quantile.probs}{Numeric vector. Optional quantiles for colour limits (overrides \code{col.limits}).}

\item{use.plotly}{Logical. Convert output to interactive \code{plotly} plot.}

\item{pltly.text}{Character. Tooltip text for interactive plots.}

\item{transformation}{Character. Scale transformation (e.g. "identity", "log10").}

\item{col.limits}{Numeric vector. Manual colour limits.}

\item{nudge_zeros}{Logical. Replace zero/low values with \code{min_nonzero}.}

\item{min_nonzero}{Numeric. Minimum value when nudging zeros.}

\item{point_layer}{sf object. Optional secondary spatial layer (e.g. points).}

\item{point_var}{Character. Variable used to colour the point layer.}

\item{point_colours}{Named vector. Colours for categorical point data.}

\item{point_scale.title}{Character. Legend title for point layer.}

\item{point.legend.size}{Numeric. Size of points in legend only (improves readability).}
}
\value{
A \code{ggplot} object, or a \code{plotly} object if \code{use.plotly = TRUE}.
}
\description{
A flexible mapping function for visualising spatial (\code{sf}) data using
\code{ggplot2}. Supports continuous and categorical polygon fills, optional
quantile-based scaling, and an optional second layer (e.g. points)
with an independent colour scale via \code{ggnewscale}.
}
\details{
Designed to be backwards compatible with earlier versions while adding
support for overlaying point data (e.g. survey points, tree locations)
on top of polygon maps.
\itemize{
\item Uses \code{ggnewscale::new_scale_colour()} to allow independent colour scales
for polygons and points.
\item Legend appearance for points is improved using \code{guide_legend(override.aes=...)}
so that small or transparent points remain visible in the legend.
}
}
\examples{
\dontrun{
map.ploter(
  fillground = sher_vn_os_pred,
  to.plot = "ha.wt.mean.NEGBINC",
  fill.scale.title = "Prediction",

  point_layer = sher_ati,
  point_var = "VeteranStatusName",
  point_colours = c("Ancient"="red","Veteran"="blue"),
  point_scale.title = "Tree status"
)
}

}
