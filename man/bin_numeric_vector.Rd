% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/U_utilities_functions.R
\name{bin_numeric_vector}
\alias{bin_numeric_vector}
\title{Bin a numeric vector using custom breaks and return a factor with readable labels}
\usage{
bin_numeric_vector(x, breaks, use_plus = TRUE, separator = "–")
}
\arguments{
\item{x}{A numeric vector to be binned.}

\item{breaks}{A numeric vector of breakpoints. Must be sorted and unique.}

\item{use_plus}{Logical. If \code{TRUE}, the final bin will be open-ended with "+" (e.g., "500+").}

\item{separator}{String used between bounds in labels. Defaults to en dash \code{"–"}.}
}
\value{
A factor assigning each \code{x} to a bin, with ordered levels and readable labels.
}
\description{
Bins a numeric vector into intervals defined by \code{breaks}, returning a factor with
labels like "0–4", "5–19", "500+" (for the final bin). Suitable for summarising or plotting.
}
\examples{
x <- c(1, 3, 10, 25, 600)
breaks <- c(0, 5, 20, 100, 500, 1000)
bin_numeric_vector(x, breaks)

}
