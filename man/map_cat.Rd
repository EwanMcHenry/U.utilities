% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/U_utilities_functions.R
\name{map_cat}
\alias{map_cat}
\title{Create a Categorical Map with ggplot2}
\usage{
map_cat(
  grid = pred.nwss.grid \%>\% sf::st_simplify(dTolerance = 100),
  var.name = "grid.vet.type",
  main.title = "NWSS veteran records",
  sub.title = NULL,
  fill.scale.title = NULL
)
}
\arguments{
\item{grid}{An \code{sf} object containing the spatial data to be visualized. Default is \code{pred.nwss.grid \%>\% st_simplify(dTolerance = 100)}.}

\item{var.name}{The name of the column in \code{grid} to be used for categorization. Default is \code{"grid.vet.type"}.}

\item{main.title}{The main title of the map. Default is \code{"NWSS veteran records"}.}

\item{sub.title}{Optional. The subtitle of the map. Default is \code{NULL}.}

\item{fill.scale.title}{Optional. Title for the fill scale (legend). Default is \code{NULL}.}
}
\value{
A \code{ggplot} object containing the generated map.
}
\description{
This function generates a categorical map using the \code{ggplot2} package to visualize spatial data. It allows for customization of titles, fill scale, and includes world map outlines (only ne countries, can edidt this).
}
